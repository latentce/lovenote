---
import { tokenizePostBody } from '../lib/post';

interface Props {
	body: string;
}

const { body } = Astro.props;
const tokens = tokenizePostBody(body);
---

<p class="whitespace-pre-wrap break-words">{
	tokens.map((token) =>
		token.kind === 'link' ? (
			<a class="font-medium text-rosewood underline decoration-rose-300 underline-offset-2 hover:text-rose-800" href={token.href} rel="nofollow ugc">
				{token.text}
			</a>
		) : (
			token.text
		),
	)
}</p>
